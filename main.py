import cv2


flag=True

facefeatures = cv2.
eyefeatures = cv2.

video = cv2.VideoCapture(0)
while flag:
    if oxFF = 'q':
        flag=False

    faceCord = facefeatures.
    for cords  in faceCord:


video.release()
cv2.destroyAllWindows()

